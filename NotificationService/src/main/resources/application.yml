spring:
  mail:
    username: kanth1997.9036@gmail.com 
#    ${mailId}
    password: 
#    ${mailPassword}
    
eureka:
  client:
    register-with-eureka:  true
    fetch-registry:  true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000
            
